apiVersion: v1
kind: Service

metadata:
  name: fitnessweb

spec:

  selector:
    app: fitnessweb

  ports:

  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.containerPort }}

  type: {{ .Values.service.type }}
namespace: fitness

replicaCount: 1

image:
  repository: acrplatformdemo123.azurecr.io/fitnessweb
  tag: latest
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8000

containerPort: 8000

resources:
  requests:
    cpu: 100m
    memory: 128Mi

  limits:
    cpu: 500m
    memory: 512Mi